# MySQL (desarrollo local o Railway)
DB_URL=jdbc:mysql://localhost:3306/desarrollo_web?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
DB_USER=TU_USUARIO
DB_PASSWORD=TU_CONTRASEÑA

# SMTP (recuperación de contraseña)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=TU_CORREO
SMTP_PASSWORD=TU_APP_PASSWORD

# Puerto HTTP (opcional en local; Railway lo define automáticamente)
PORT=8080
